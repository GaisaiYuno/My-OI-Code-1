#include <cstdio>
using namespace std;
int n, d, k;
int main() {
	freopen("jump.in", "r", stdin);
	freopen("jump.out", "w", stdout);
	scanf("%d %d %d", &n, &d, &k);
	for (int i = 1; i <= n; i++) {
		int x, s;
		scanf("%d %d", &x, &s);
	}
	printf("-1");
	return 0;
}

#include<iostream>
#include<stdio.h>
using namespace std;
int main()
{
	freopen("jump.in","r",stdin);
	freopen("jump.out","w",stdout);
	cin>>n>>d>>k;
	for(i=1;i<=n;i++)
	{
		scanf("%d%d",&a[i].x,&a[i].y);
		if (a[i].y>0) e+=a[i].y;
	}
	if (e<k) 
	{cout<<-1;return 0;}
	int s=0;
	for(i=1;i<=n;i++)
	{
		if (a[i].y<0)
		{
		    if ()
	    }
	}
	return 0;
}

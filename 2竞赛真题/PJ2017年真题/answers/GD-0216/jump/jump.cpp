#include<iostream>
using namespace std;
struct v1
{
 int x,s;

}	a[500001];
struct v2{
	int i,l
}s2[500001];

int n,d,k,sum,i,j,cnt,s[500001],g,s1[500001];
boolmap(v2 a,v2 b)

int main()
{
	cin>>n>>d>>k;
	for(i=1;i<=n;i++)
	{
		cin>>a[i].x>>a[i].s;
		if(a[i]>0)
		{
		sum+=a[i];
		cnt++;
		s[cnt]=i;
		g=max(g,abs(s[cnt]-[cnt-1]));
		s2[cnt].i=cnt;
		s2[cnt].l=abs(s[cnt]-[cnt-1]);
		}
		
	}
if(sum<k)
cout<<"-1";
else
if(sum==k)cout<<g;
else{
	re=sum-k;
	while(re-a[s2[cnt].i]0&&s2[cnt]==g)cnt--,rest-=a[s2[cnt].1];
	for(i=1;i<=cnt;i++)
	int tmp=100000000;
	for(j=[sa[s2[cnt].i-1]];j)
}
}

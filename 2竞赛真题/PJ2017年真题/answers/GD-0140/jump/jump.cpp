#include <iostream>
#include <cstdio>

using namespace std;

int a[2005][3];

int main () {
	freopen ("jump.in", "r", stdin);
	freopen ("jump.out", "w", stdout);
	
	int n, d, k;
	cin >> n >> d >> k;
	
	for (int i = 1; i <= n; i++)
	
	fclose (stdin);
	fclose (stdout);
	return 0;
}
